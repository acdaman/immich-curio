services:
  curio:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - TZ=Australia/Sydney
    networks:
      - postgres-net

networks:
  postgres-net:
    external: true
